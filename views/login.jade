extends layout

block content
  form.login(method="POST")
    div.third
      h2(style="text-align:center;") #{title} 
      input.stack(type='text', placeholder='注意是id不是名字哦' name='id' required value=(undefined===user ? '' : user.id) )
      input.stack(type='password',placeholder='密码' name='password' required value = (undefined===user ? '' : '*')) 
      button.stack.icon-paper-plane(type='submit',style="text-align:center;") 提交
  div(style="height:50px")